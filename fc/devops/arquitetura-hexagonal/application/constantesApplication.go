package application

const (
	DISABLED         = "disabled"
	ENABLED          = "enabled"
	INVALID_STATUS   = "invalid"
	PRICE_ZERO       = "the price must be greater than zero to enable the product"
	PRICE_NOT_ZERO   = "the price must be zero in order to have the product disabled"
	STATUS_NOT_VALID = "the status must be enabled or disabled"
	PRICE_NOT_VALID  = "the price must be greater or equal zero"
)
